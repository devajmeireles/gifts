<?php

use App\Http\Livewire\Setting\Index;
use App\Models\Setting;

use function Pest\Laravel\get;
use function Pest\Livewire\livewire;

beforeEach(fn () => createTestUser());

it('can render component')->todo();

it('can list')->todo();

it('can search')->todo();

it('can paginate')->todo();

it('can change quantity')->todo();

it('can sort')->todo();

it('can filter for empty results')->todo();

